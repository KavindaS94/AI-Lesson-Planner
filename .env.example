# Server-only — never prefix with NEXT_PUBLIC_. Get a free key from https://aistudio.google.com/apikey
GEMINI_API_KEY=...

# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=...
BETTER_AUTH_URL=http://localhost:3000
